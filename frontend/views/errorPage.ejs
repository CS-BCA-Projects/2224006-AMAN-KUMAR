<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Unauthorized Access</title>
    <link rel="stylesheet" href="/css/custom.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        *{
            font-family: 'Roboto',Arial, Helvetica, sans-serif;
        }
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
            color: #0d6efd;
            text-align: center;
            font-family: 'Arial', sans-serif;
            padding: 20px;
        }
        .error-code {
            font-size: 150px;
            font-weight: bold;
            color: #0d6efd;
        }
        .error-message {
            font-size: 30px;
            margin-bottom: 20px;
            color: #343a40;
        }
        .btn-custom {
            font-size: 18px;
            padding: 10px 20px;
            margin: 5px;
            border-radius: 5px;
            width: auto;
        }
        @media (max-width: 768px) {
            .error-code {
                font-size: 100px;
            }
            .error-message {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="error-code">404</div>
    <h2 class="error-message">Unauthorized Access</h2>
    <p class="text-muted">You do not have permission to view this page.</p>
    <a href="/" class="btn btn-primary btn-custom">Go to Homepage</a>
    <a href="/api/v1/login" class="btn btn-outline-primary btn-custom">Login</a>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
